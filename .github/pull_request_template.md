## Change

What problem does this solve? Note any numerical or observable behavior change.

## Validation

List commands run and results. Mark checks that do not apply as N/A.

- CPU/source checks:
- CUDA/reference and seeded-generation checks, if relevant:
- Paged KV, cancellation/page reuse and graph topology/pointer lifetime, if relevant:

## Performance

State "no performance claim", or link paired A/B evidence with the GPU,
power/clock envelope, configuration, round order, medians and ranges.
